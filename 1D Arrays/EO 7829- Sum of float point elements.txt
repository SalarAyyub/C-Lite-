#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin>>n;

    float a[n], sum=0;

    for(int i=0; i<n; i++)
    {
        cin>>a[i];
        sum+=a[i];
    }

    printf("%0.1f", sum);

    return 0;
}

